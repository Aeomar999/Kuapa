---
description: Image-first pipeline — generate site references, analyze them, then implement the frontend to match.
---

Load and apply the `image-to-code` skill from `.opencode/skills/image-to-code/`. Execute its full instructions — generate design images, deeply analyze them, then implement the website to match them as closely as possible.