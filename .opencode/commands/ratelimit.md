---
description: Production-grade rate limiting for vibe-coded projects. Add throttling, abuse prevention, and API protection.
---

Load and apply the `rate-limiter` skill from `.opencode/skills/rate-limiter/`. Execute its full instructions — detect the stack, classify endpoints, choose the algorithm, and implement complete production-ready rate limiting.
