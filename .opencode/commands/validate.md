---
description: Universal input validation guard. Scans and secures every data entry point in your project.
---

Load and apply the `input-validator` skill from `.opencode/skills/input-validator/`. Execute its full instructions — scan the project stack, inventory all input surfaces, generate validation schemas for every endpoint, add required tests, and wire up global error handling for validation failures.
