---
description: Code audit and cleanup specialist that identifies and fixes AI-generated slop in vibe-coded projects.
---

Load and apply the `slop-detector` skill from `.opencode/skills/slop-detector/`. Execute its full instructions — run the slop audit framework, identify critical/quality/cleanup issues, and provide refactored code.
