{
  "name": "bexiemart-root",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "build": "npm --prefix apps/server install --legacy-peer-deps && npm --prefix apps/server run prisma:generate && npm --prefix apps/server run build",
    "start": "npm --prefix apps/server run start:prod"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.29.7",
    "@babel/preset-typescript": "^7.29.7",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "apps/mobile/**/*.{js,jsx,ts,tsx}": [
      "prettier --write"
    ],
    "apps/server/**/*.{js,ts}": [
      "prettier --write"
    ]
  }
}
