{
  "name": "bexiemart-root",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "apps/mobile/**/*.{js,jsx,ts,tsx}": [
      "prettier --write"
    ],
    "apps/server/**/*.{js,ts}": [
      "prettier --write"
    ]
  }
}
